h1 유저 리스트
p db.csv에서 읽은 유저 리스트입니다
p #{users.length} 명의 유저입니다.

ul
    for user in users
        h3 #{user.userId} #{user.name}
        p #{user.email}
        a(href="/users/" + user.userId) 상세 페이지 바로가기

extends ../base

block content
    h1 new post

    form(action="/posts/" method="POST")
        input(type="text" name="title" placeholder="제목")
        input(type="text" name="content" placeholder="본문")
        inpnut(type="submit" value="글 작성하기")